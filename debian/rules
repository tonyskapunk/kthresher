#!/usr/bin/make -f

export PYBUILD_DESTDIR_python2=debian/python-kthreser/
#export PYBUILD_DESTDIR_python3=debian/python3-kthresher/

%:
#  dh $@ --with python2,python3 --buildsystem=pybuild
	dh $@ --with python2 --buildsystem=pybuild

